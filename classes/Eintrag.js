"use strict";

class Stock_Eintrag{

    constructor(id, name, kategorie, preis, freite, datum, anzahl){

        this._id = id ;
        this._name = name ;
        this._kategorie = kategorie ;
        this._preis = preis ;
        this._freite = freite ;
        this._datum = datum ; 
        this._anzahl = anzahl ;

     }   
    
}